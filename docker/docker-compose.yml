services:
  mongodb:
    image: mongo:8.2.1-noble
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - ./mongo-data:/data/
    networks:
      - backend
    restart: always

networks:
  backend: